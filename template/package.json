{
  "name": "{{ name }}",
  "version": "1.0.0",
  "description": "{{ description }}",
  "author": "{{ author }}",
  "private": true,
  "scripts": {
    "build": "node build_script/build.js",
    "deploy": "node build_script/deploy.js"
  },
  "dependencies": {

  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-clean": "^0.3.1",
    "gulp-sequence": "^0.4.6",
    "gulp-less": "^3.3.0",
    "gulp-concat": "^2.6.0",
    "gulp-print": "^2.0.1",
    "gulp-uglify": "^2.0.0"
  }
}